<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenmingqiang.wirelesssim.user.infrastructure.UserMapper">

    <resultMap id="userAccountResultMap" type="com.chenmingqiang.wirelesssim.user.domain.UserAccount">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="role" column="role" javaType="com.chenmingqiang.wirelesssim.user.domain.UserRole"/>
        <result property="status" column="status" javaType="com.chenmingqiang.wirelesssim.user.domain.UserStatus"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="userAccountResultMap">
        SELECT id, username, password_hash, role, status, created_at, updated_at
        FROM app_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.chenmingqiang.wirelesssim.user.domain.UserAccount"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (username, password_hash, role, status)
        VALUES (#{username}, #{passwordHash}, #{role}, #{status})
    </insert>
</mapper>
